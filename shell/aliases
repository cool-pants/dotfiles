# basic
alias cls='clear'
alias cl='clear'
alias scl='clear'
alias la='eza -al'
alias lla='eza -al'

# Run from it hide from it
alias vi='nvim'
alias nv='nvim'
alias emacs='nvim'
alias vim='nvim'
alias nano='nvim'
alias e='nvim'
alias gedit='nvim'
alias code='nvim'
# Extremely common typo
alias nivm="nvim"
# It always has been vim

alias pyac='source $(pwd)/venv/bin/activate'
alias back='cd -'

# Navigation
alias ..='cd ..'
alias ....='cd ../..'

# git
alias g='git'
alias gr='git remote'
alias gra='gr add'
alias gpo='git pull origin'
alias gs='git status'
alias gc='git commit'
alias grb='git rebase'

# kubernetes
alias k='kubectl'
alias k3='k3 kubectl'

# Node
alias cra='npx create-react-app'

# Package specific
# Hyper charge cd with zoxide
alias cd='z'

# For fonts to work in tmux
alias tmux="TERM=$TERM tmux -u"
